# Agent Instructions

- Treat `/Users/binam/Pictures/Photos Library.photoslibrary` as a strictly read-only data source. Inspect files inside that package if needed, but never modify or write anything there.
